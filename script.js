//your JS code here. If required.

function name(params) {
	for(let i=0;i<param.length();i++){
		if(param.indexOf(param.charAt(i))==param.lastIndexOf(param.charAt())){
			alert(i);
			break;
		}
	}
}

 promt("Enter a String");
alert(name(str));
